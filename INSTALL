Installation instructions for the calcCMD command line calculator

calcCMD is available to Arch users on the Arch User Repository

Requirements:
GNU Make
A bash shell
The GNU Compiler Collection
Root privileges (sudo)

To install calcCMD from source:

$ make
# make install
$ make clean

This will build the program, placing the executable in /bin
The clean operation will remove the object files to free disk space.

You can now run the program with `$ calccmd`
